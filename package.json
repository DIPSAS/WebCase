{
  "name": "dips-interview-case",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "check-types": "turbo run check-types"
  },
  "packageManager": "pnpm@10.17.0",
  "devDependencies": {
    "prettier": "^3.6.0",
    "turbo": "^2.6.1"
  },
  "engines": {
    "node": ">=24",
    "pnpm": ">=10"
  }
}
